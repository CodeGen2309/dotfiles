# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor = , preferred, auto, auto
monitor = HDMI-A-1, 1920x1080, 0x0, 1
monitor = DVI-I-1, 1920x1080, 1920x0, 1

workspace = 1, monitor:HDMI-A-1, default:true
workspace = 6, monitor:DVI-I-1, default:true

###################
### MY PROGRAMS ###
###################

permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
# $fileManager = nemo
# $terminal = alacritty


source = ./wallpaper.conf

$terminal = alacritty
$fileManager = alacritty -e ranger
$btop = alacritty -e btop
# $menu = rofi -show drun -show-icons
$menu = walker
$setpaper = kitty -e sh ~/scripts/setpaper.sh



#################
### autostart ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# To mount usb and disks automatically
exec-once = udiskie

# app manager
exec-once = walker --gapplication-service

exec-once = /usr/lib/polkit-kde-authentication-agent-1
exec-once = hypridle
exec-once = mako 
exec-once = hyprpaper
exec-once = waybar
exec-once = pamac-tray-plasma
exec-once = $setpaper $paper


#############################
### environment variables ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XDG_SESSION_TYPE, wayland
env = XDG_CURRENT_DESKTOP, Hyprland
env = XDG_SESSION_DESKTOP, Hyprland

env = HYPRCURSOR_THEME, ~/.icons/ArcStarry-cursors
env = HYPRCURSOR_SIZE, 24

env = XCURSOR_THEME, ~/.icons/posy_cursor
env = XCURSOR_SIZE, 24

env = QT_QPA_PLATFORMTHEME, qt6ct
env = QT_QPA_PLATFORM, wayland
env = GDK_BACKEND, wayland
# env = WLR_RENDERER, vulkan



#####################
### look and feel ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
	gaps_in = 10
	gaps_out = 20
	border_size = 2

  # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
  # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
	col.active_border = rgb(2ecc71)
	col.inactive_border = rgb(006266)

  # Set to true enable resizing windows by clicking and dragging on borders and gaps
	resize_on_border = true

  # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
	allow_tearing = false
	layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
	rounding = 6

	# Change transparency of focused and unfocused windows
	active_opacity = 1.0
	inactive_opacity = 0.8

  # drop_shadow = true
  # shadow_range = 4
  # shadow_render_power = 3
  # col.shadow = rgba(1a1a1aee)

  # https://wiki.hyprland.org/Configuring/Variables/#blur
	blur {
		enabled = true
		size = 10
		passes = 2

		vibrancy = 0.1696
	}
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
	enabled = true

  # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

	bezier = myBezier, 0.05, 0.9, 0.1, 1.05
	bezier = myTest, 1, 0.55, 0, 0.45
	bezier = bounce, 1,1.6,0.1,0.85
	bezier = slingshot, 1,-2,0.9,1.25

	animation = windows,  1, 5, myBezier, popin 70%
	animation = windowsOut, 1, 10, slingshot, popin 90%
	animation = border, 1, 10, default
	animation = borderangle, 1, 8, default
	animation = fade, 1, 7, default
	animation = workspaces, 1, 6, default, slidefade 10%

  # animation = windows,  1, 10, default, popin 90%
  # animation = windows, 1, 5, default, slide bottom 80%
  # animation = windowsOut, 1, 7, default, popin 80%
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
	preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
# master { new_is_master = true }
# pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
	force_default_wallpaper = 1  # Set to 0 or 1 to disable the anime mascot wallpapers
	disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### input ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
	kb_layout = us, ru
	kb_variant =
	kb_model =
	kb_options = grp:win_space_toggle
	kb_rules =

	follow_mouse = 1
	sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

	touchpad {
		natural_scroll = false
	}
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
	workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
	name = epic-mouse-v1
	sensitivity = -0.5
}


####################
### keybindings ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = Alt, Space, exec, $terminal
bind = $mainMod, L, exec, killall mpv; hyprlock


# custom keys
# --------------------------------------------------------------------------
bind = $mainMod, X, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, Return, exec, $menu
bind = $mainMod, N, exec, nemo
bind = $mainMod, P, exec, nemo ~/Pictures/
bind = $mainMod, B, exec, $btop

# bind = $mainMod, P, pin, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# bind = , PRINT, exec, hyprshot -m output
bind = , PRINT, exec, hyprshot -m window
bind = SHIFT, PRINT, exec, hyprshot -m region

bind = $mainMod, Q, exec, walker -m power
bind = $mainMod, R, exec, walker -m radio
bind = $mainMod, Z, exec, zen-browser
bind = $mainMod SHIFT, R, exec, alacritty -e sudo reboot now


# Move focus with mainMod + arrow keys
# ----------------------------------------------------
bind = Alt, left, movefocus, l
bind = Alt, right, movefocus, r
bind = Alt, up, movefocus, u
bind = Alt, down, movefocus, d



# Move between workspaces
# ----------------------------------------------------
bind = $mainMod, left, workspace, r-1
bind = $mainMod, right, workspace, r+1




# Switch workspaces with mainMod + [0-9]
# useless for me
# ----------------------------------------------------
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10


# Move active window to a workspace with mainMod + SHIFT + [0-9]
# ----------------------------------------------------
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10
bind = $mainMod, f, fullscreen, 1


# Swap windows in workspace
# ----------------------------------------------------
bind = $mainMod SHIFT, Left, swapwindow, l
bind = $mainMod SHIFT, Right, swapwindow, r
bind = $mainMod SHIFT, Up, swapwindow, u
bind = $mainMod SHIFT, Down, swapwindow, d


# Resize windows
# ----------------------------------------------------
bind = $mainMod control, Left, resizeactive, -20 0
bind = $mainMod control, Right, resizeactive, 20 0
bind = $mainMod control, Up, resizeactive, 0 -20
bind = $mainMod control, Down, resizeactive, 0 20


# Example special workspace (scratchpad)
# ----------------------------------------------------
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic


# Scroll through existing workspaces with mainMod + scroll
# ----------------------------------------------------
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1


# Move/resize windows with mainMod + LMB/RMB and dragging
# ----------------------------------------------------
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow


# Volume up/down
# ----------------------------------------------------
bind = control SHIFT, Up, exec, pactl set-sink-volume 0 +10%
bind = control SHIFT, Down, exec, pactl set-sink-volume 0 -10%


##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules
windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.



### TAGMAP
##############################

windowrulev2 = tag +minfloat,  class:^org.kde.gwenview
windowrulev2 = tag +minfloat,  class:^Vivaldi-stable
windowrulev2 = tag +minfloat,  class:.*qBittorrent.*
windowrulev2 = tag +minfloat,  class:pamac-manager
windowrulev2 = tag +minfloat,  class:^sublime_text
windowrulev2 = tag +minfloat,  class:^zen

windowrulev2 = tag +minfloat,  title:^Alacritty
windowrulev2 = tag +minfloat,  title:^Flatseal

windowrulev2 = tag +minfloat,  class:^mpv
windowrulev2 = tag +notrans,   class:^mpv

windowrulev2 = tag +minfloat,  class:^obsidian
windowrulev2 = tag +litetrans, class:^obsidian

windowrulev2 = tag +minfloat,  class:^nemo
windowrulev2 = tag +litetrans, class:^nemo

windowrulev2 = tag +minfloat,  class:^org.kde.discover
windowrulev2 = tag +litetrans, class:^org.kde.discover

windowrulev2 = tag +litetrans, class:^code-oss
windowrulev2 = tag +centered,  class:^PortProton




### TAGLIST
##############################

#tag minfloat
#------------------------------------------------------
windowrulev2 = float,              tag:minfloat
windowrulev2 = center 1,           tag:minfloat
windowrulev2 = size 1200 600,      tag:minfloat


#tag transparent
#------------------------------------------------------
windowrulev2 = opacity 1 override,   tag:notrans
windowrulev2 = opacity 0.9 0.7 override, tag:litetrans


#tag centered
#------------------------------------------------------
windowrulev2 = float,    tag:centered
windowrulev2 = center 1, tag:centered



# krita rules
#------------------------------------------------------
windowrulev2 = minsize 800 400, title:^Open Images
windowrulev2 = center 1,  title:^Open Images
windowrulev2 = minsize 800 400, title:^Save Image on Split
windowrulev2 = center 1,  title:^Save Image on Split
